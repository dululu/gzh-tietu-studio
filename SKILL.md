---
name: gzh-tietu-studio
description: 公众号「贴图」（图片消息）出图流水线：一份 JSON 数据 → 810×1080（精确 3:4）信息图，9 种版式 + 5 套配色，自动配图（维基共享 / NASA / Pexels），可批量出 PNG、可导出可编辑 SVG、可生成九宫格总览。当用户要求做贴图、出贴图、做信息图、给热点配图、批量出图、导出 Figma 可编辑版、生成封面图/海报时使用。
agent_created: true
---

# 公众号贴图工坊（gzh-tietu-studio）

一份 JSON → 一批 810×1080 信息图。**同一份骨架，只换内容区**：水彩底 + 白色圆角卡片 + 双行主标题（第二行带雾带）+ 右上角账号角标 + 底部来源。

## 什么时候用

- 「做几张贴图 / 图片消息 / 信息图 / 海报」
- 「把这条热点做成图」「给这段数据配张图」
- 「批量出图」「导出成能改的矢量图」「生成封面」

不适用：需要真实照片当主视觉且不接受示意图的场合（开放图库多为通用题材）；需要逐像素手工设计的封面。

## 环境要求

- **Node.js**（只用到内置模块，无 npm 依赖）
- **Chrome / Edge / Chromium**（无头截图用；脚本内置多路径探测）
- **macOS**：用系统自带 `sips` 压缩图片（非 macOS 会跳过压缩，功能不受影响）
- 可选：Pexels API key（想用现代实拍照片时；维基共享和 NASA 免 key）

## 三条主命令

```bash
# 1) 批量出 PNG（最常用）
./批量出图.sh 数据.json 输出目录

# 2) 导出可编辑 SVG（拖进 Figma 改配色/间距/加元素）
node 导出SVG.js 数据.json 输出目录

# 3) 生成九宫格总览（一次看完一批，先自查再发）
node 生成总览.js 数据.json 输出目录 "标题"
```

配图取图（免费可商用，自动打印作者+许可）：

```bash
node 找图.js search "humanoid robot" 8      # 维基共享 · 全文搜
node 找图.js cat "Humanoid robots" 8        # 维基共享 · 按分类（更准）
node 找图.js get "File:xxx.jpg" img 1600    # 下载并打印署名信息
node 找图.js nasa "mars rover" 8            # NASA 图库（公有领域）
node 找图.js pexels "campus library" 8      # Pexels（要 key，现代实拍感最好）
```

## 动手顺序（按这个顺序做，别跳）

1. **定选题 → 选版式**。只问一个问题：要给读者看的是「一件东西」「一组数」还是「一个比较」？
   `stat` 单数字 / `list` 清单 / `compare` 对比 / `timeline` 时间轴 / `chart` 柱线 / `bar` 条形 /
   `photo` 单图 / `photoFocus` 圆形焦点 / `duo` 双主体+四宫格。详见 `docs/说明书.md` 第三节。
2. **写数据 JSON**。每张图一个对象，`name` 决定输出文件名，其余字段透传给模板。
   字段速查见 `docs/说明书.md` 第五节。
3. **配图**（只有 `photo` / `photoFocus` / `duo` 需要，`duo` 的四宫格图可选）。
4. **出图 → 回读自检**。必须逐张看图，不能只看命令行说成功。
5. **要二次设计再走 SVG**。

## 铁律（踩过的坑，务必遵守）

1. **出图后要验证"各图字节数互不相同"**。`stat -f "%z" 输出目录/*.png | sort | uniq -c`。
   9 张一样大＝其实渲染的是同一个页面（多半是模板选错了），比白屏更难发现。
2. **截图必须写临时文件名再 mv**。目标文件已存在时，"文件大小稳定"会立刻成立，脚本误判完成、
   拿到上一轮的旧图。脚本已经这么做了，别改回去。
3. **图片字段的路径按「包含 img」匹配**，不能按「以 img 开头」——`quadImgs` 开头是 `quad`。
4. **图片路径写相对于数据源 JSON 所在目录**（脚本会依次尝试：数据源同目录 → 脚本目录 → 当前工作目录）。
5. **署名是使用条件，不是礼貌**。CC BY / CC BY-SA 必须署名作者；CC0 / 公有领域不强制。
   图片一多（`duo` 最多 6 张）只列必须署名的几个，许可名可以合并成一组。**脚注最多 3 行**
   （一行约 41 个汉字 / 76 个西文字符），超了会折行、把内容区顶小。
6. **CC BY-SA 有「传染性」**：裁一刀就算改编，改编稿要以同样许可发布。统一处理＝照常裁剪 +
   在 `foot` 里补一行说明（单张写 `经裁剪改编，本图按相同许可 CC BY-SA 3.0 发布`；混许可并进许可行）。
   能选就优先 CC BY / CC0 / 公有领域；商业投放改走 Pexels 或自采。
7. **别把开放图库的图当"现场照"**。通用题材图配国内大学题时，图注写"示意图"，不写"某校现场"。
8. **文案长度红线**：副标题 ≤ 15 汉字；`statLabel` ≤ 19 汉字；`compare` 每栏要点 ≤ 12 汉字
   （超了会折行、两栏参差）。写长了字会被模板自动缩小，观感变差。

## 文件说明

| 文件 | 作用 |
|---|---|
| `贴图模板.html` | 出图模板本体（浏览器直接打开可当设计台用，有版式/配色下拉） |
| `主题.js` | 5 套配色定义，HTML 与 SVG 两条路共用这一份 |
| `批量出图.js` | 批量生成页面 + 无头 Chrome 截图（纯 Node） |
| `批量出图.sh` | 同上，但是"Node 生成页面 → Shell 调 Chrome"两段式（沙箱里 Node 起不了 Chrome 时用这个） |
| `导出SVG.js` | 导出可编辑 SVG，本地图会压成 base64 内嵌 |
| `找图.js` | 维基共享 / NASA / Pexels 三个图源的搜索与下载 |
| `生成总览.js` | 九宫格总览页 |
| `vendor/echarts.min.js` | 图表版式依赖（必须保留） |
| `docs/说明书.md` | **完整中文说明书**（安装、全字段、9 版式详解、配图流程、FAQ） |
| `docs/贴图排版手册.md` | 排版原理与设计规范（标题公式、骨架常量、12 个坑） |
| `examples/` | 9 版式示例数据 + 真实热点示例数据 + 示例配图 + 输出预览 |

## 深入阅读

- 要写数据、查字段、看某个版式怎么用 → `docs/说明书.md`
- 要改版式、改配色、调间距、加新模板 → `docs/贴图排版手册.md`
