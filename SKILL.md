---
name: gzh-tietu-studio
description: 公众号「贴图」/海报出图流水线：一份 JSON 数据 → 810×1080（精确 3:4）信息图，16 种版式 + 5 套配色 + 满幅外壳（拆掉白卡、底图铺满画布），自动配图（维基共享 / NASA / Pexels / AI 生成底图），可批量出 PNG、可导出可编辑 SVG、可生成总览。当用户要求做贴图、出贴图、做信息图、给热点配图、批量出图、导出 Figma 可编辑版、生成封面图/海报时使用。
agent_created: true
---

# 公众号贴图工坊（gzh-tietu-studio）

一份 JSON → 一批 810×1080 信息图。**同一份骨架，只换内容区**：水彩底 + 白色圆角卡片 + 双行主标题（第二行带雾带）+ 右上角账号角标 + 底部来源。

> ### 📖 完整说明书：[`说明书.md`](说明书.md) ← 第一次用先读这个
>
> 12 节，从装到用全覆盖：环境准备 / 16 种版式逐个讲 / 三个图源怎么找图 / 全字段速查表 /
> 导出 SVG / 九宫格总览 / 常见问题 / 踩坑清单。**本文件只是一张速查卡，细节都在说明书里。**
>
> 要改模板、改配色、调间距、加新版式 → [`docs/贴图排版手册.md`](docs/贴图排版手册.md)
>
> 想知道"海报感"从哪来、还有哪些手法没做 → [`docs/视觉海报案例学习笔记.md`](docs/视觉海报案例学习笔记.md)

## 什么时候用

- 「做几张贴图 / 图片消息 / 信息图 / 海报」
- 「把这条热点做成图」「给这段数据配张图」
- 「批量出图」「导出成能改的矢量图」「生成封面」

不适用：需要真实照片当主视觉且不接受示意图的场合（开放图库多为通用题材）；需要逐像素手工设计的封面。

## 环境要求

- **Node.js**（只用到内置模块，无 npm 依赖）
- **Chrome / Edge / Chromium**（无头截图用；脚本内置多路径探测）
- **macOS**：用系统自带 `sips` 压缩图片（非 macOS 会跳过压缩，功能不受影响）
- 可选：Pexels API key（想用现代实拍照片时；维基共享和 NASA 免 key）

## 几条主命令

```bash
# 1) 批量出 PNG + 顺带体检（最常用，一条命令就够）
./批量出图.sh 数据.json 输出目录
#    结果里直接印「✅ 放得下 … 图 6/6 张」。溢出检查与真图清点已经并进这一趟，
#    不用再单独跑校验脚本。

# 2) 回头重看上次的体检结论（读 .pages/.dumps 缓存，一次浏览器都不启）
./检查溢出.sh 输出目录

# 3) 导出可编辑 SVG（拖进 Figma 改配色/间距/加元素）
node 导出SVG.js 数据.json 输出目录
node svg校验.js 输出目录          # 可选：量 SVG 真实边界，查文字有没有顶出画布/盖住脚注

# 4) 生成九宫格总览（可选项，默认不用出）
node 生成总览.js 数据.json 输出目录 "标题"
./总览截图.sh 输出目录            # 存成 _总览.png（自动量页高，不留白不截断）
```

配图取图（免费可商用，自动打印作者+许可）：

```bash
node 找图.js search "humanoid robot" 8      # 维基共享 · 全文搜
node 找图.js cat "Humanoid robots" 8        # 维基共享 · 按分类（更准）
node 找图.js get "File:xxx.jpg" img 1600    # 下载并打印署名信息
node 找图.js nasa "mars rover" 8            # NASA 图库（公有领域）
node 找图.js pexels "campus library" 8      # Pexels（要 key，现代实拍感最好）
```

官方媒体图（写时事热点时的正路，受《著作权法》二十四条约束，见说明书 4.5）：

```bash
node 找图.js page "<文章URL>" "文件名" 1600   # 抓 og:image 主图，并打印标题/媒体/记者
node 找图.js url  "<图片直链>" "文件名" 1600   # 页面抓不到主图时的兜底
```

挖中国题材 + 补署名：

```bash
node 探测分类.js "Classrooms in China" "Factories in China"   # 哪些分类有图
node 列分类.js "Classrooms in China"                          # 列出该分类全部文件名
node 查署名.js "File:xxx.jpg"                                  # 补查作者与许可
node 署名行.js 数据源.json                                     # 生成脚注署名行 + 报字宽
```

## 动手顺序（按这个顺序做，别跳）

1. **定选题 → 选版式**。只问一个问题：要给读者看的是「一件东西」「一组数」「一个比较」还是「一步步怎么走」？
   纯文字类：`stat` 单数字 / `list` 清单 / `compare` 对比 / `timeline` 时间轴 /
   `vs` 对峙对比 / `cards` 行动卡 / `steps` 四步阶梯 / `bigword` 巨型字 / `band` 色带分割；
   数据类：`chart` 柱线 / `bar` 条形；
   带图类：`photo` 单图 / `photoFocus` 圆形焦点 / `duo` 双主体+四宫格 / `hero` 大图压标题 /
   `cover` 满幅封面（自带满幅，用 `bg` 底图）。
   **`bigword` 和 `stat` 的分界**：讲数字用 `stat`，讲一个**词**（撤销 / 缺人）用 `bigword`。
   **`band` 和 `cards` 的分界**：要"分别要干什么"用 `cards`，要"并列的几类、一眼扫完"用 `band`。
   **`bigword` 和 `cover` 的分界**：都想要满幅就用 `cover`（拆白卡、藏共用标题）；只想要"那个字"当主角、
   其余保持信息图样子就用 `bigword`。
   **另外还有一个和版式正交的开关**：`"shell": "full"` —— 拆掉白卡、底图铺满画布，
   **16 个版式都能加**。不写 `bg` 时得到"无卡的纯色满幅"（`band` 配这一档最好看）。
   详见 `说明书.md` 第三节。
2. **写数据 JSON**。每张图一个对象，`name` 决定输出文件名，其余字段透传给模板。
   字段速查见 `说明书.md` 第五节。
3. **配图**（`photo` / `photoFocus` / `duo` / `hero` 用 `img`，`cover` 用满幅底图 `bg`，
   `shell:"full"` 也可选配 `bg`；`duo` 的四宫格图可选）。没有合适实拍时可用 AI 生成底图，
   但**脚注必须标「示意图」**且要人眼验有没有伪文字。
4. **出图 → 回读自检**。必须逐张看图，不能只看命令行说成功。
5. **要二次设计再走 SVG**。

## 铁律（踩过的坑，务必遵守）

1. **出图后要验证"各图字节数互不相同"**。`stat -f "%z" 输出目录/*.png | sort | uniq -c`。
   整批一样大＝其实渲染的是同一个页面（多半是模板选错了），比白屏更难发现。
   这一条现在出图时会自动报（见第 9 条），不用再手敲。
2. **截图必须写临时文件名再 mv**。目标文件已存在时，"文件大小稳定"会立刻成立，脚本误判完成、
   拿到上一轮的旧图。脚本已经这么做了，别改回去。
3. **图片字段的路径按「包含 img」匹配**，不能按「以 img 开头」——`quadImgs` 开头是 `quad`。
   **但满幅底图 `bg` 里不含 `img`，必须在白名单里显式列出来**——出图脚本的绝对化、
   `度量.js` 的 `expectImgs`、`署名行.js` 的收集条件，**三处都要加**。漏了页面不报错、
   底图只是静静地 404，症状是体检打「图 0/0 张」：看着像"探针不认识新字段"，其实是**图真的没加载**。
4. **图片路径写相对于数据源 JSON 所在目录**（脚本会依次尝试：数据源同目录 → 脚本目录 → 当前工作目录）。
5. **署名是使用条件，不是礼貌**。CC BY / CC BY-SA 必须署名作者；CC0 / 公有领域不强制。
   **脚注只写三样：来源 + 作者 + 许可名** —— `配图：维基共享资源 · N509FZ；CC BY-SA 4.0`。
   图片一多（`duo` 最多 6 张）只列前 2 个作者 + 「等」，许可名合并成一组。**脚注最多 3 行**
   （一行约 41 个汉字 / 76 个西文字符），超了会折行、把内容区顶小。
   这行不必手写：`node 署名行.js 数据源.json` 从图反查台账拼好，并报出占多少字宽。
6. **别把许可条款原文抄进脚注**。CC BY-SA 照常裁剪使用，但「经裁剪，按相同许可发布」这类话
   读者看不懂——它是要留档的事实，记在 `配图署名.md` 台账里（每行都有来源与许可），
   需要追溯改编与再许可时查台账，不占脚注位置。
   能选就优先 CC BY / CC0 / 公有领域；商业投放改走 Pexels 或自采。
7. **别把开放图库的图当"现场照"**。通用题材图配国内大学题时，图注写"示意图"，不写"某校现场"。
   **要中国场景，用带 `in China` 的维基共享分类去挖，别用中文全文搜**——搜「小学」会跨语言匹配到
   波兰、赞比亚的小学。先用 `node 探测分类.js "Classrooms in China" ...` 试哪些分类有料，
   再用 `node 列分类.js` 列全部文件名挑；Pexels 基本只有欧美/东南亚场景。
   **微博 / 小红书 / B站 / 百度图片不要直接扒**——没有开放接口，且默认保留全部权利；
   要用就走"自己截图 / 官方媒体图标来源 / 拿到授权"这三条路，详见说明书 4.1.1。
   官方媒体图走 `node 找图.js page "<文章URL>"`，**但受《著作权法》二十四条四要件约束**
   （为报道该时事新闻 / 不可避免 / 指明作者与来源 / 不损害权利人权益），拿它当无关话题的"气氛图" ❌。
8. **文案长度红线**：副标题 ≤ 15 汉字；`statLabel` ≤ 19 汉字；`compare` 每栏要点 ≤ 12 汉字
   （超了会折行、两栏参差）。写长了字会被模板自动缩小，观感变差。
   `hero` 的 `heroTitle` ≤ 14 汉字、`heroItems[].p` ≤ 14 汉字（图下三张卡很窄，一格只有 178px）。
   **`bigword` 的 `bwWord` 只放 2–4 汉字**（要讲一句话就别用这个版式）、`bwNote` ≤ 34 汉字。
   **`band` 的 `bands[].p` ≤ 26 汉字**（4 条时每条只有 139px 高，折到第二行就溢出）、条数 2–4 条。
   加了 `shell:"full"` 后内容区变高，3 条色带实测各 261px，红线可放宽 —— 但 4 条仍要验。
   **`cover` 的 `cvBig` ≤ 16 汉字**（82px，658px 宽下每行 8 个汉字 × 2 行，含 `<em>` 里的字）。
   它**故意不接 `fitText`**、按自然折行排，所以没有"写长了自动缩小"这层保护 ——
   超出会由 `越界探针.js` 报出来。**宁可报错，不要静默把封面大字缩小。**
   `cvNote` ≤ 40 汉字、`cvKeys` 放 2–3 项、每项 `v` ≤ 12 汉字。
9. **溢出检查 + 真图清点已经并在出图那一趟里了**。页面注入了只读探针（`度量.js`），截图时多要一份
   `--dump-dom`，「放不放得下 / 真图几张 / 有没有缺图」跟着截图一起回来，跑完直接印在结果里
   （`✅ 放得下 … 图 6/6 张`）。**校验不再是额外一趟**：以前一批 6 张图光校验就 21s，现在零趟。
   想回头重看上次的结论：`./检查溢出.sh 输出目录` —— 读缓存，不启浏览器。
   结果里出现 `⚠️ 溢出` 才是问题：`duo` 这类高度写死的版式会把内容顶到页脚上，肉眼看图几乎发现不了。
10. **`duo` 的四宫格默认给满四张 `quadImgs`**。只给 `quads` 标签会退回「首字大卡」（四个色块各压一个
    大号汉字），那是兜底样式、不是常用样式。要空就四格一起空。宫格近方形（约 152×142），竖图居中裁
    只损失约 30%（跟 1.5:1 横图的 28% 差不多），**能放**；反倒是 `duo` 主体画框 320×272 且
    `object-position: top center`，**16:9 宽幅图只留 66%**，主体得落在上半部。
    **注意 `duo` 一共 6 张图，署名行会挤爆 3 行脚注**——只列主体图作者 + 「等」，
    完整名单写进项目的配图署名台账。
11. **总览是可选项**（默认不用出）。要出就跑 `node 生成总览.js` → `./总览截图.sh 输出目录`，
    别写死 `--window-size` 高度。总览页高随张数变化，
    写死不是底部留白就是截掉最后一行。脚本先量 `documentElement.scrollHeight` 再截；量高与截图必须同宽。
    张数不是 9 的时候用 `--cols=N` 调列数（5 列时截图宽度取 1938）。
12. **验收带图版式时，中心切片只能当提示**。`photoFocus` 正中本来就被白圆盖住、白色实验室/教室场景
    本身就压得小，都会误报成"图没进去"。**决定性做法是 A/B**：把 `img` 换成不存在的路径再出一张，
    比较两张 PNG 的总字节数（实测 1482 KB vs 870 KB）。另外 `src="/` 数出来的真图数，
    `duo` 补了 `quadImgs` 之后应该显示 `图 6/6 张`——这个数现在出图时自动报，对不上会直接标出来。
13. **新增版式是纯增量，但两条路要一起补**。`贴图模板.html` 加 CSS + `LAYOUTS` 键 + `<option>`，
    `度量.js` 的 `expectImgs` 补期望图数，**`导出SVG.js` 的 `CONTENT` 也要补一个 `contentXxx`**。
   SVG 那条路以前是 `CONTENT[d.layout] || CONTENT.photo` **静默回退成 photo**，不报错、
   导出结果和 PNG 对不上——现在已经会警告，但警告只说明"你漏了"，该补还得补。
   另外 SVG 的 `<text>` **不自动折行**，长句要么调短、要么用 `TBlock()` 自己切行。
   **补完必须验"纯增量"**：重出后老图 md5 要和改动前**逐字节相同**，否则说明新 CSS 碰坏了旧版式。
14. **要"等高 / 等宽"的装配，装饰线一律别用 `border`**。`band` 的留白带原来用 `border-top/bottom: 2px`，
   实测三条色带变成 **173 / 177 / 173**（中间多 4px），即使全局有 `box-sizing: border-box` 也一样。
   改用 `box-shadow: inset 0 2px 0 …, inset 0 -2px 0 …` 后回到 175/175/175。`box-shadow` 不参与布局。
15. **`fitText` 只在"父容器显式撑满"时才生效**。`bigword` 的巨型字容器 `.bw-box` 在
   `align-items: center` 的 flex 列里会收缩到内容宽，`el.parentElement.clientWidth` 于是等于字自己的宽度，
   "放得下"永远成立 → 4 个字的巨型字不缩字号、直接横着顶出画布。**给容器加 `width: 100%` 才修好。**
16. **设计内的满幅出血元素要打 `.bleed`**。`bigword` 的色块垫和 `band` 的色带都左右各出 50px
   （从 x=26 顶到 x=784，和背景装饰 blob 同类），`越界探针.js` 会把它们全报成越界。
   加 `.bleed` 类进白名单即可。**新增任何出血元素时记得同时打上这个类**，否则探针就废了。
   白名单的判定要用 **`el.closest('.bleed')` 而不是 `classList.contains`** —— 探针遍历的是
   `.content *` 的**每一个**元素，出血容器**里面的**子元素（`.t` / `.ck`）同样在卡外，
   只跳过容器本身会把它们全报出来。
17. **满幅（`shell:"full"`）只换背景层，不动布局**。`.poster.full .card { background: transparent }`
   + `.poster.full { border-radius: 0 }`，但 `inset: 26px` / `padding: 50px` **全留着** ——
   所以 `.content` 的边界和原来一模一样，**溢出检查和越界探针原封不动继续有效**，
   不用为满幅另写一套校验。三个类分开管：`.full`（拆卡）/ `.hasbg`（有底图才挂淡纱）/
   `.own-title`（`cover` 用，藏掉共用双行标题）。
18. **满幅会改出血元素的位置，两条渲染路必须一起改**。卡片外边距 26px 也没了，所以
   `bigword` 的色块垫、`band` 的色带要从 `x=26, w=758` 变成 `x=0, w=810`。
   HTML 那条路是 CSS（`.poster.full .ly-band .bd { margin: 0 -76px; padding: 0 76px }`），
   **`导出SVG.js` 那条路是 `contentBigword()` / `contentBand()` 里写死的 `rect(PAD, …)`**，
   两边必须走同一个 `bleedBox(d)`。漏改的后果是"PNG 顶到 0、SVG 停在 26"——**两份产物都渲染成功、
   谁也不报错**，只有把两侧的 `画布[…]` 数值并排看才发现。验收：白卡形态应是
   `[26,26 → 784,1010]`、满幅形态应是 `[0,26 → 810,1010]`。
19. **AI 生成底图要先人眼过一遍有没有伪文字**。模型在中英混排提示下常画出"看着像汉字其实是乱码"
   的装饰字，且往往在角落。目前没有自动检查手段，**生成后第一件事就是打开看一眼**。
   另外 AI 图一律按「示意图」入脚注（`署名行.js` 认「AI 生成」这个许可族，会生成
   `配图：AI 生成底图（示意图）；无第三方著作权`），**不能暗示成现场照**。
   生成时按 **2:3 竖图（1024×1536）** 出，别按 16:9 —— 画布是 3:4，2:3 只裁掉约 11% 高度。

## 文件说明

| 文件 | 作用 |
|---|---|
| `贴图模板.html` | 出图模板本体（浏览器直接打开可当设计台用，有版式/配色下拉） |
| `主题.js` | 5 套配色定义，HTML 与 SVG 两条路共用这一份 |
| `度量.js` | 出图时顺带体检：溢出量 / 真图数 / 缺图数写进页面 `<title>`，随截图一起回来 |
| `批量出图.js` | 批量生成页面 + 无头 Chrome 截图（纯 Node），多张并发 |
| `批量出图.sh` | 同上（并发 + 顺带体检），但是"Node 生成页面 → Shell 调 Chrome"两段式（沙箱里 Node 起不了 Chrome 时用这个） |
| `导出SVG.js` | 导出可编辑 SVG，本地图会压成 base64 内嵌；未登记的版式会告警不再静默兜底 |
| `svg校验.js` | 量导出 SVG 的真实边界（`getBBox`），查文字顶出画布 / 盖住脚注 |
| `越界探针.js` | 量页面里每个元素的 rect，抓 `检查溢出.js` 抓不到的「看得见地顶出卡片」；`.bleed` 是设计内出血白名单（用 `closest` 判定，连子元素一起跳过） |
| `海报探针.js` | 读 `computedStyle` / `naturalWidth`，确认字号 / 色块 / 倾斜 / 台阶 / **满幅底图有没有真的解码** 生效（而不是被 `fitText` 缩回去、或图片静静 404） |
| `找图.js` | 维基共享 / NASA / Pexels 三个图源的搜索与下载；`page` / `url` 抓官方媒体图 |
| `查署名.js` | 按 Commons 标题补查作者与许可（只读元数据，不下载图片） |
| `署名行.js` | 从数据源扫出真正用到的图（含满幅底图 `bg`）→ 查台账 → 拼好脚注那行署名，并报出占多少字宽 |
| `探测分类.js` / `列分类.js` | 挖中国题材用：批量试哪些 Commons 分类有图，以及列出某个分类的全部文件名 |
| `生成总览.js` | 总览页（只出 HTML），`--cols=N` 调列数 |
| `总览截图.js` / `总览截图.sh` | 把总览页存成 PNG：先量真实页高再截，不留白不截断 |
| `预览截图.sh` | 把 `svg输出_*/_预览.html` 存成 PNG（`./预览截图.sh svg输出_样板 [宽] [倍率]`）。带 `--user-data-dir` 的截图 Chrome 不会自己退出，这个脚本封装了"后台起进程 + 轮询文件大小稳定再强杀" |
| `检查溢出.js` / `检查溢出.sh` | 体检模块 + 重看结论（`from-dumps` 读缓存、一次 Chrome 都不启；没缓存才回退探针） |
| `vendor/echarts.min.js` | 图表版式依赖（必须保留） |
| `说明书.md` | **完整中文说明书**（主入口：安装、全字段、16 版式详解、配图流程、FAQ） |
| `docs/贴图排版手册.md` | 排版原理与设计规范（骨架常量、满幅外壳、标题公式、海报化视觉手段、23 个坑） |
| `docs/视觉海报案例学习笔记.md` | 视觉海报精选案例拆解：每条的出处、落到哪个版式、哪些故意没做 |
| `examples/` | 16 版式样板集（18 张，覆盖全部 16 种版式）+ 真实热点示例数据 + 示例配图（含 `img/ai/` 两张 AI 底图）+ 输出预览 |

## 深入阅读

- 要写数据、查字段、看某个版式怎么用 → `说明书.md`
- 要改版式、改配色、调间距、加新模板 → `docs/贴图排版手册.md`
- 想知道"海报感"是怎么做出来的、下一轮还能加什么 → `docs/视觉海报案例学习笔记.md`
