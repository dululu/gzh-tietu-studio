# 说明书 · 公众号贴图工坊

> **这是全套文档的主入口。** 从安装到出图、从字段到配图，都在这一份里。
>
> | 想看什么 | 去哪 |
> |---|---|
> | 怎么装、怎么跑、字段怎么填、配图怎么找、遇到问题怎么办 | **就是本文**（往下读） |
> | 改模板 / 改配色 / 调间距 / 了解排版原理 | [`docs/贴图排版手册.md`](docs/贴图排版手册.md) |
> | 效果预览、项目简介 | [`README.md`](README.md) |
> | 作为 WorkBuddy 技能使用 | [`SKILL.md`](SKILL.md) |
> | 示例图片的作者与许可 | [`examples/CREDITS.md`](examples/CREDITS.md) |

---

## 一、它是什么

把一份 JSON 数据（每张图一个对象）批量渲染成 **810×1080** 的公众号贴图。数据变了版式不用重做，
因为十七种版式共用同一套骨架：

```
水彩底（径向渐变 blob）
└─ 白色圆角卡片 810×1080，inset 26px，圆角 26
   ├─ 右上角账号角标 88×88          ← `badge`
   ├─ 主标题两行 72px / 58px        ← `title1` `title2`（第二行带雾带）
   ├─ 副标题 32px（可选）            ← `deck`
   ├─ 内容区                         ← `layout` 决定（十七选一）
   └─ 底部来源注释 16px 灰           ← `foot`
```

白色卡片**可以整张拆掉**：数据里加一行 `"shell": "full"`，白卡消失、底图铺满整张画布、圆角归零，
内容区边界却一点不动（所以体检和溢出检查照常有效）。这是**和版式正交的开关**，十七种版式都能加 ——
详见 [`docs/贴图排版手册.md`](docs/贴图排版手册.md) 第一节的「满幅外壳」。

产物规格：CSS 810×1080，导出 2 倍图 **1620×2160**（3:4，公众号图片消息的竖版比例）。

---

## 二、安装与运行环境

### 环境要求

| 依赖 | 是否必须 | 说明 |
|---|---|---|
| Node.js ≥ 16 | 必须 | 只用内置模块，**不需要 npm install** |
| Chrome / Edge / Chromium | 必须 | 无头截图；脚本会依次探测常见安装路径 |
| macOS `sips` | 可选 | 压缩下载的图片；非 macOS 自动跳过 |
| ECharts | 已内置 | `vendor/echarts.min.js`，只有 `chart` / `bar` 两个版式用到 |

### 两种用法

**A. 当普通工具用（推荐）**：把仓库 clone 到任意目录，`cd` 进去执行命令即可。

```bash
git clone <本仓库> && cd gzh-tietu-studio
./批量出图.sh examples/数据-样板集.json out    # 15 张，覆盖 14 种版式
```

`examples/` 里还有两份更小的：`数据-九种版式示例.json`（最基础的 9 种）和
`数据-热点示例.json`（一份真实热点素材，含全部字段写法）。

> 样板集是 **15 张、覆盖 14 种版式** —— `photoFocus` / `band` / `circle` 三种**暂无样张**
> （用户评审时撤下了），但**版式和字段都在**，照着字段表填一样能出图。

**B. 当 WorkBuddy 技能用**：把整个目录放进 `~/.workbuddy/skills/gzh-tietu-studio/`，
之后用自然语言说「做几张贴图」就会自动加载本技能。脚本支持从任意工作目录调用——
**素材（模板、vendor、示例数据）从技能目录读，数据和产物落在你当前的项目目录里**。

### 各条命令的适用场景

| 命令 | 产物 | 什么时候用 |
|---|---|---|
| `./批量出图.sh 数据.json 输出目录` | PNG 2 倍图 + 体检结论 | **默认走这条**。终端直接调 Chrome，绕开沙箱对子进程的限制；多张并发，并顺带把体检指标捎回来 |
| `node 批量出图.js 数据.json 输出目录` | PNG 2 倍图 + 体检结论 | 本机权限正常时的纯 Node 版本，行为一致 |
| `node 导出SVG.js 数据.json 输出目录` | 可编辑 SVG | 要换配色、调间距、加元素、做系列变体时 |
| `./检查溢出.sh 输出目录` | 命令行结论 | 重看已出那批的体检结果，**读缓存、一次 Chrome 都不启**；没缓存才逐张回读。逐张量"内容区放不放得下"，防止内容顶到页脚上 |
| `node 生成总览.js 数据.json 输出目录 "标题"` | `_总览.html` | **可选**。想一次看完一批的整体观感（`--cols=N` 改列数） |
| `./总览截图.sh 输出目录` | `_总览.png` | **可选**。把总览页存成图（自动量页高，不留白不截断） |
| `./预览截图.sh svg输出目录` | `_预览.png` | **可选**。把导出的 SVG 预览页存成图（同上做法） |
| `node svg校验.js svg输出目录` | 命令行结论 | 导出 SVG 后量真实几何边界，报顶出画布 / 盖脚注 / 压标题 |
| `node 越界探针.js .pages` | 探针页路径 | 要抓"元素看得见地顶出卡片"（体检抓不到这类）；**生成后还要逐个 `--dump-dom` 读 `<title>`** |
| `node 海报探针.js .pages` | 探针页路径 | 确认海报手法（字号 / 色块 / 倾斜 / 出血）真的生效，同上两步 |
| `node 查署名.js "File:<Commons标题>"` | 作者 / 许可 | 补查配图署名（只读元数据，不下载图片） |
| `node 署名行.js 数据.json` | 脚注署名行 | 生成 `配图：维基共享资源 · 作者；CC BY-SA 4.0` 这行，并报出占多少字宽 |

如果 `批量出图.js` 报「没权限启动 Chrome」，就用 `.sh`，反之亦然。两者共用同一份模板和配色。

**出图那一趟就把体检做完了**：页面里注入了一段只读的度量探针（`度量.js`），截图时多要一份
`--dump-dom`，于是「放不放得下 / 真图渲染了几张 / 有没有缺图」跟着截图一起回来，跑完直接印在
结果里。以前这三项要各起一趟 Chrome（一批 6 张图光校验就 21s），**现在校验是零趟**。
`./检查溢出.sh` 随时能把上次的结论重看一遍——读缓存，不启浏览器。

体检量的是模板里那个固定高度：标题两行 + 副标题 + 页脚先占位，剩下的才归内容区。
内容元素一多（典型是 `duo` 的双主体图 + 金句 + 四宫格）就会顶出去、盖住页脚——**看图很难发现，量一下就很明确**。

---

## 三、17 种版式怎么选

选版式只需要回答一个问题：**你要给读者看的是「一件东西」「一组数」「一个比较」还是「一步步怎么走」？**

| 版式 | 长什么样 | 什么时候用 | `layout` |
|---|---|---|---|
| **单数字冲击** | 一个巨大数字占满内容区，下面挂说明 | 全篇只有 1 个数字是重点（133.31 万、录取率） | `stat` |
| **编号清单** | 编号卡片列表 1/2/3 条 | 结论是"几个信号 / 几个变化 / 几条建议" | `list` |
| **左右对比** | 左浅卡右深卡，各挂 4 条要点 | A 和 B 二选一（省内 vs 外省、新高考 vs 老高考） | `compare` |
| **对峙对比** | 左右两栏各压一个**结论词**，中间 VS 徽章，底部补"适合谁" | 两类人 / 两条路二选一，且**能各用一个词总结**（先宽后窄 vs 一步到位） | `vs` |
| **编号行动卡** | 大号**实心色块**编号 + 标题 + 右上角倾斜动作标签，卡片可标警示红 | 结论是"这三件事分别要干什么"（要办 / 留意 / 别信） | `cards` |
| **四步阶梯** | 卡片逐级右移 34px 形成台阶，末级主题色反白 | 有先后推进关系，且**末级是落点**（从下车到入住） | `steps` |
| **时间轴** | 左侧年份 + 中间竖线 + 右侧事件 | 政策沿革、发展脉络 | `timeline` |
| **双轴柱线图** | 柱线双轴，末柱高亮 | 两个指标随时间走，要突出某一年 | `chart` |
| **横向条形 + 数字卡** | 横向条形，右侧并排数字对比卡 | 结论是"谁比谁多"，且要一个冲击性对比 | `bar` |
| **单图 + 图注** | 一张大图 + 一句图注 | 有一张能说明一切的图 | `photo` |
| **主图 + 圆形焦点** | 图上叠一个 480px 白圆，圈住要强调的信息 | 图里有具体数字/条件，不圈出来读者会漏 | `photoFocus` |
| **双主体 + 金句 + 四宫格** | 两个主体并置 + 一句金句 + 四格（格子里可放图） | 抽象判断要落地（"他布局了哪 4 个方向"） | `duo` |
| **大图压标题** | 大图铺满内容区，底部渐变上压一句结论，图下挂三张短要点卡 | 有一张好图，且**结论本身就是最好的标题** | `hero` |
| **巨型字主视觉** | 一个 210px 巨型字吃掉整行、下半段压满幅色块垫、整块转 -3° | 结论能压到**一个短词**，想让读者先记住"那个字"（撤销 / 缺人 / 没赶上） | `bigword` |
| **横向色带分割** | 画面切成 2–4 条满幅出血、零间距、等高的横向色带 | 条目**并列且要一眼扫完**，想让整张图显得"满"（三类清退方向、三条原因） | `band` |
| **满幅封面** | 底图铺满整张画布，顶部小标签、下部左对齐大字（带行内高亮）、底部一条满幅数字带 | 想做**封面 / 头图**性质的单张，或这句话本身够硬、不需要一堆要点托着 | `cover` |
| **正负形圆形分割** | 一个深色大圆把版面切成「圆内 / 圆外」：圆里压**反白主信息**（小标签 + 巨型词 + 说明），圆外四角压角注 | **一句话 + 一组「从哪几个角度看」的角注**就能说清的事（一个大数 / 一个词 + 四角提示） | `circle` |

另外有一个**和版式正交的开关**：`"shell": "full"`（满幅外壳）。写它就拆掉白卡、让底图铺满整张画布 ——
**任何版式都能加**（`cover` 自带，不用写；`circle` 也自带**整版排版**但不带满幅，想要就照常写），
详见 [`docs/贴图排版手册.md`](docs/贴图排版手册.md) 第一节。

**选择顺序**：先看手上有什么素材（有图 → `photo` / `photoFocus` / `duo` / `hero` / `cover`；
纯数字 → `stat`/`chart`/`bar`/`circle`；纯文字 → `list`/`cards`/`compare`/`vs`/`steps`/`timeline`/`bigword`/`band`），
再看想让读者记住什么（画面 → `photo`/`hero`/`cover`；数字 → `stat`/`bar`/`circle`；观点 → `duo`/`list`；
顺序 → `timeline`/`steps`；二选一 → `compare`/`vs`；
**一个字 → `bigword`；一张"满"图 → `band`；记住一句狠话 → `cover`；记住"一个大数 + 四角怎么看" → `circle`**）。

> **`compare` 和 `vs` 怎么选**：`compare` 是"两条各 4 条要点，你自己比"；
> `vs` 是"两条路我先各用一个词总结给你"。要点多、要罗列 → `compare`；
> 要点少、能提炼 → `vs`（呼应更好）。
> **`list` 和 `cards` 怎么选**：`list` 是竖着扫一列编号；`cards` 每张卡是封闭单元，
> 右上角的动作标签直接告诉读者"这条要干什么"。
> **`timeline` 和 `steps` 怎么选**：`timeline` 是一条竖线串起的**时间顺序**；
> `steps` 强调"一步比一步更靠后"的推进关系，末级反白当落点。
> **`stat` 和 `bigword` 怎么选**：`stat` 是"一个数字 + 说明"的信息图（数字最大 170px，靠字号取胜）；
> `bigword` 是"一个字就是画面主体"的海报（210px + 色块垫 + 倾斜），**数字只是顺带**。
> 要大字讲**数字** → `stat`；要大字讲**词** → `bigword`。
> **`cards` 和 `band` 怎么选**：`cards` 是有间距的圆角卡片，一条一条看；
> `band` 是零间距的满幅色带，一眼扫完。要"分别要干什么" → `cards`；要"并列的几类" → `band`。
> **`bigword` 和 `cover` 怎么选**：`bigword` 是**卡内**的巨型字（还留着白卡、顶部还有双行标题），字是主角；
> `cover` 是**整张画布**的海报（白卡拆了、共用标题也藏了），底图和那句大字一起当主角。
> 想让"那个字"单独跳出来 → `bigword`；想整张就是一张封面 → `cover`。
> **`band` 和 `cover` 怎么选**：`band` 是**多条并列**的色带（3 条起才有"分割"的意思）；
> `cover` 底部只有**一条**数字带，是收口不是分割。
> **`bigword` 和 `circle` 怎么选**：`bigword` 的字就是画面主体（210px，可以只写 1 个字），别处不放信息；
> `circle` 的主信息在**圆里**（118px，可带单位），四角还各留一条角注。
> 只想砸一个字 → `bigword`；**一个大数还不够、还想说清"从哪几个角度看"** → `circle`。
> **`cover` 和 `circle` 怎么选**：两个都自带整版排版、都藏共用标题。
> `cover` 的主角是**底图 + 一句大字**（靠 `bg`）；`circle` 的主角是**那个大圆**（靠色块不靠图）。
> 要"一张封面照片" → `cover`；要"一个色块把版面切开" → `circle`。

### 文案长度红线（写长了字会被自动缩小，观感变差）

| 位置 | 上限 |
|---|---|
| `deck` 副标题 | ≤ 15 汉字 |
| `statLabel` | ≤ 19 汉字 |
| `compare` 每栏要点 | ≤ 12 汉字（两栏各占半宽，超了必折行、两栏参差） |
| `title1` / `title2` | 各 ≤ 12 汉字，超了靠 `fitText` 缩字号 |
| `quads` 四宫格标签 | 各 ≤ 10 汉字，模板会逐格缩到单行 |
| `vs` 两侧结论词 `vsLeftTag` / `vsRightTag` | 各 ≤ 5 汉字（药丸很窄，超了会被缩小） |
| `vs` 两侧小标题 `vs*Title` | 各 ≤ 5 汉字 |
| `vs` 每侧要点 `vs*Items[]` | 各 ≤ 11 汉字（单侧宽约 240px） |
| `vs` 底部"适合谁" `vs*Fit` | ≤ 12 汉字 |
| `cards` 卡的标题 `h` | ≤ 14 汉字（右边还要放动作标签） |
| `cards` 卡的解释 `p` | ≤ 25 汉字 / 行，写长了自动折第二行 |
| `steps` 每步 `h` | ≤ 6 汉字（和左侧「时间词」同一行） |
| `steps` 每步 `p` | ≤ 24 汉字 / 行 |
| `hero` 压图结论 `heroTitle` | ≤ 14 汉字（54px，两边各留 30px） |
| `hero` 图下小卡 `heroItems[].h` | ≤ 8 汉字（一格只有 178px 可用宽） |
| `hero` 图下小卡 `heroItems[].p` | ≤ 14 汉字（会折第二行） |
| `bigword` 巨型字 `bwWord` | **2–4 汉字最佳**。宽了会自动缩字号（4 字缩到约 165px），超过 6 字会缩到下限后顶出画布 |
| `bigword` 单位 `bwUnit` | ≤ 4 字符（如 `万个`、`%`） |
| `bigword` 说明 `bwNote` | ≤ 34 汉字（超了折行，图变挤） |
| `bigword` 数字块 `bwKeys[].k` / `.v` | `k` ≤ 5 字符、`v` ≤ 8 汉字 |
| `band` 色带标题 `bands[].h` | ≤ 10 汉字 |
| `band` 色带解释 `bands[].p` | **≤ 26 汉字**。4 条时每条只剩 139px 高，折到第二行就溢出 |
| `band` 条数 `bands` | 2–4 条（超过 4 条每条太矮，正文放不下） |
| `cover` 顶部小标签 `cvTag` | ≤ 16 汉字（22px 单行，超了缩字号） |
| `cover` 大字 `cvBig` | **每行 8 汉字、共 2 行（合计约 16 汉字）**。**这一条不接 `fitText`**：写超了不会缩字号，会直接折第三行然后顶出画布——`越界探针.js` 会报出来，**要自己守字数** |
| `cover` 说明 `cvNote` | ≤ 40 汉字（23px，会折第二行） |
| `cover` 数字带 `cvKeys[].k` / `.v` | `k` ≤ 5 字符、`v` ≤ 12 汉字（块宽有限，`v` 会缩字号） |
| `circle` 圆内小标签 `ccKick` | ≤ 12 汉字（22px 单行，超了缩字号） |
| `circle` 圆内主词 `ccWord` | **1 个词或一个数**，2–4 字符最佳。会和单位一起量宽，超了自动缩字号（下限 56px） |
| `circle` 圆内单位 `ccUnit` | ≤ 5 字符（如 `所高校`、`通识课`） |
| `circle` 圆内说明 `ccNote` | ≤ 22 汉字（21px，宽 434px，会折第二行） |
| `circle` 四角角注 `ccCorners[].h` | ≤ 6 汉字（21px 单行，超了缩字号；**满幅下要压到 5 字以内**） |
| `circle` 四角角注 `ccCorners[].p` | **≤ 2 行**：卡内一行约 11 汉字（共 22 字）、**满幅一行只有约 7 汉字（共 14 字）**。超 2 行会把角注顶高压进圆里——`海报探针.js` 会打出 `角注离圆=-xx px` |
| `circle` 角注条数 | 1–4 条（`tl` / `tr` / `bl` / `br`）。四角都放最平衡；只放 2 条建议走对角线（`tl` + `br`） |

> **`circle` 是唯一「满幅反而更窄」的版式**：满幅时圆要顶到画布边，四角就被压小
> （角注宽从卡内的 208px 变成 150px），**角注文案得按满幅收**。上表「满幅宽一档」对它不适用。

> **满幅（`"shell": "full"`）会让上表（除 `circle` 外）全部宽一档**：白卡拆掉后内容区从 CARD_TOP 一路放到脚注，
> 纵向多出约 60px。最受益的是 `band`——白卡形态下 3 条各 175px，满幅实测 **261px**，
> `bands[].p` 可以从 26 汉字放宽到 **32 汉字**左右。`cvBig` 是例外：它横向占的是画布宽，
> 满幅只多出一点点，**字数红线不变**。

---

## 四、配图

### 4.1 图源怎么选

| 图源 | key | 适合什么 | 命令 |
|---|---|---|---|
| **维基共享资源** | 免 | **首选**，题材最广，每张都带作者+许可 | `search` / `cat` / `get` |
| **NASA Image Library** | 免 | 航天、机器人、地球影像，公有领域 | `nasa` / `nasa-get` |
| **Pexels** | 要 | 现代实拍感最好，**但基本都是欧美/东南亚场景**；配国内题材会"不像我们的学校" | `pexels` / `pexels-get` |
| 大都会 / 克利夫兰 / 芝加哥美术馆 | 免 | 艺术与历史文物；科技题材基本没有，适合「古文字学 / 非遗 / 考古」 | 未内置 |
| **AI 生成（本地）** | — | 只用来做**满幅底图**这种"底纹"性质的图；开放图库找不到合适的抽象/氛围底时用它兜底。`img/ai/` 下已有两张现成 | 见 4.4 第 4 条 |

实测过但本机连不上的：Openverse（连接失败）、Library of Congress（Cloudflare 403）。
Pixabay / Unsplash / Smithsonian 接口通但要 key，Pexels 够用就没接。

### 4.1.1 想找「中国题材」的图？（重要）

Pexels 的现代实拍感最好，但**几乎都是欧美/东南亚场景**——配国内题材会显得"不是我们的学校"。
要中国场景，正确做法是**用中文关键词和中文分类去维基共享里挖**，那里的中国摄影师非常多：

```bash
node 找图.js search "汉服" 6                     # 中文关键词直接可用（脚本会做 URL 编码）
node 找图.js search "石室中学" 6                 # 学校名、地名、地标都能搜
node 找图.js cat "Hanfu" 6                       # 分类树比全文搜更准
node 找图.js cat "Textile industry in China" 6
```

> ⚠️ **中文全文搜会被跨语言匹配带偏**。搜「小学」返回的可能是波兰、法国、赞比亚的小学——
> 因为 CirrusSearch 会把「小学」匹配到各国"小学"条目上。要中国场景，**优先用带 `in China` 的分类**。

分类树是这儿的主力工具。先用 `探测分类.js` 一批批试，看哪个分类下面真有东西：

```bash
node 探测分类.js "Classrooms in China" "Rural classrooms in China" "Factories in China"
node 探测分类.js "Universities and colleges in Beijing" "Laboratories in China"
```

输出会列每个分类的文件数、子分类和示例文件名；文件数是 0 就换个名字再试
（分类名必须精确，`Primary schools in China` 是空的、`Classrooms in China` 才有内容）。
一旦找到有料的分类，用 `列分类.js` 把它连同子分类的文件名全部列出来挑：

```bash
node 列分类.js "Classrooms in China" "Factories in China"     # 清单同时写到 /tmp/分类清单.txt
```

实测好用的方向（都是中国摄影师的自由授权作品）：

| 想找什么 | 分类（比全文搜准） | 常见作者 |
|---|---|---|
| 中小学教室、课堂 | `Classrooms in China`、`Rural classrooms in China` | Kychn（铁岭中学）、Rex Pe（广西民族学校） |
| 中国大学校园 | 校名分类，如 `Xiamen University`、`Wuhan University` | Windmemories、天王星、EricDiao |
| 高校实验室、国家重点实验室 | `Laboratories in China` | WFan 等 |
| 养老院、敬老院 | `Nursing homes in Guangzhou` / `in Sanya` | PQ77wd、N509FZ |
| 工厂、工业园区 | `Factories in China`、`Industrial parks in China` | Vmenkov、kingwill |
| 汉服、汉服社活动 | `Hanfu`、`Hanfu of the 21st century` | Allervous、Nan Ning Huang Sokaz 12 |
| 传统纺织、织机 | `Weaving in China`、`蜀锦`、`Sichuan Brocade` | Kcx36、Gary Todd（多为 CC0）、CEphoto |

两条经验：
- **维基按宽度取缩略图会限流（HTTP 429）**。撞上就降一档宽度（1600 → 1200 → 1000 → 800）重试，
  别在同一尺寸上反复试；`找图.js` 自己会重试两次，仍失败就换宽度。
- **顺手记下作者和许可**。取图时 `找图.js` 会打印，务必抄进 `foot`；漏了可以用
  `node 查署名.js "File:<Commons标题>"` 补查（只读元数据，不用重新下载）。

**为什么不能直接从微博 / 小红书 / B站 / 百度图片拿图：** 这些平台没有公开的检索下载接口
（所以 `找图.js` 里没有它们），而且图片默认保留全部权利。公众号里用了，属于侵权风险，跟"标了来源"无关。
真要用的只有三条路：

1. **自己截图**（用户素材）——最省事，也是本工具一直在用的方式。把图丢进项目 `img/`，
   JSON 里写 `"img/xxx.jpg"` 即可，脚本不关心图是哪来的。责任归属也很清楚。
2. **官方媒体的新闻图**（新华社 / 人民日报 / 地方党媒）——时事新闻报道有合理使用的空间，
   但**商业投放仍然有风险**，务必标注来源，且不要裁掉人家水印之外的署名信息。
3. **拿到授权**——找权利人或其经纪/学校宣传部门要图，最稳。

高校官网、政府网站的宣传图同理：最贴切，但仍受版权保护，标来源只是降低风险、不等于取得授权。
**优先顺序**：自由授权图 > 自己截图 > 官方媒体图（标来源）> 其它。

不管用哪条路，**别把图说成"现场照"**：通用题材一律在图注或脚注写「示意图」（见 4.4 第 3 条）；
AI 生成的图另有三条纪律（见 4.4 第 4 条）。

```bash
# 维基共享资源
node 找图.js search "humanoid robot" 8      # 全文搜（自动滤掉竖图和小图）
node 找图.js cat "Humanoid robots" 8        # 按分类取，比全文搜准
node 找图.js get "File:xxx.jpg" img 1600    # 下载，打印作者/许可/许可链接/原页

# NASA
node 找图.js nasa "robot arm space station" 8
node 找图.js nasa-get "iss071e361950" img 1600

# Pexels（key 见 4.3）
node 找图.js pexels "university students campus" 8
node 找图.js pexels-get "8199659" img 1600
```

下载默认落到**当前工作目录的 `img/`**，也会自动按需压缩（原图只比目标宽一点点时不重编码，
否则体积反而变大——`sips` 默认质量比原图高）。

**偶发超时是正常的**：维基把缩略图挪到了新的 `thumb.wikimedia.org`，本机到它时不时 ETIMEDOUT。
脚本的接口查询和下载都带 3 次重试 + socket 超时，失败时会删掉半截文件（留着 0 字节同名图最坑）。

### 4.2 带图版式的选图要求

| 版式 | 画面位置 | 选图建议 |
|---|---|---|
| `photo` | 满宽大图，约 658×600（横构图） | 主体在中间偏上，裁掉上下都不影响 |
| `photoFocus` | 中间被 480px 白圆盖住 | **主体不要放正中**，让有辨识度的部分留在圆外一圈 |
| `duo` 主体 | 两张方图，各 320×290，按顶部对齐裁 | 主体靠上；两张色调别差太远 |
| `duo` 四宫格 | 四个小方块，各约 152×148（**接近 1:1**） | 会裁掉横图两侧；主体要在中央，或配 `quadPos` 挪进画面 |
| `hero` | 满宽大图，约 658×420（≈1.6:1），**底部 64% 会压渐变遮罩 + 白字** | 主体放**上半部**；照片下半部会变暗，别选"重点在下半张"的构图 |
| `bg`（满幅底图） | **铺满整张 810×1080 画布**，上面还盖一层"上白下白、中间透"的淡纱和一句大字 | **选虚化 / 大留白 / 没有具体主体的图**。它不是"展示内容的图"，是**底纹**——画面里有个清晰人脸或一堆密集文字，压上大字就全毁了 |

**画框宽高比决定竖边裁多少**（全部 `object-fit: cover`，只看原图长宽比 vs 画框）：

| 画框 | 4:3 图 | 3:2 图 | 16:9 图 | 2:3 竖图 | 3:4 竖图 |
|---|---|---|---|---|---|
| `photo` / `photoFocus`（658×600） | 留 82% | 留 72% | 留 62% | — | **只留 54%** ❌ |
| `hero`（658×420） | 留 83% | 留 88% | 留 78% | — | 留 62% |
| `duo` 主体（320×272，顶对齐） | — | — | **只留 66%**且靠上 ❌ | — | — |
| `duo` 四宫格（152×142，近方形） | 留 81% | 留 72% | — | **留 70%，竖图没问题** | — |
| `bg` 满幅（810×1080，3:4） | **只留 56%** | **只留 50%** | **只留 42%** ❌ | 留 89% | **留 100%** ✅ |

> 想让照片少挨刀，就把画框做高一点。**两个硬坑**：`photo` / `photoFocus` 配 3:4 竖图
> （竖的全身照会被从腰部截断，这种图改用 `duo` 四宫格或 `hero`）；
> **`bg` 配横图**（画布是竖的 3:4，16:9 横图只留 42%，等于把左右两端全砍掉）。
> `bg` 最优是 **3:4 或 2:3 竖图**。

最容易翻车的三处：

- **`photoFocus`**：白圆几乎盖满画面，选一张主体正好在中间的图（比如人像特写），圆一压就全没了。
- **`hero`**：底部 64% 压着渐变遮罩，选一张"重点在上半部"的图；重点全在下半张的话会被字吃掉。
- **`bg`**：它是**底纹**，不是配图。有清晰主体（人脸、招牌、密集文字）的图压上大字就是一团糟；
  顶部 94% 和底部 88% 白纱最厚，**中间那段最透** —— 想让人看见的内容要落在画面中部。

**排版心得（实测对比过）**：`duo` 版式里，**大图放"场景"、小图放"特写"**。
小格子只有 152px，场景图（人 + 环境）缩下去就糊成一片；特写（一个电极帽、一片晶圆）反而认得出来。

### 4.3 Pexels key

key 不写进脚本，按顺序查找：环境变量 `PEXELS_API_KEY` → `~/.workbuddy/pexels.key` → 项目内 `.pexels-key`。

```bash
echo "你的key" > ~/.workbuddy/pexels.key && chmod 600 ~/.workbuddy/pexels.key
```

放在**用户目录**而不是项目目录，是为了不跟着项目被提交/分享出去。key 在 <https://www.pexels.com/api/> 免费申请。

### 4.4 署名与许可（重要）

**署名是许可的使用条件，不是礼貌。** 规则：

| 许可 | 能商用 | 必须署名 | 备注 |
|---|---|---|---|
| CC BY | ✅ | ✅ | 可裁剪 |
| CC BY-SA | ✅ | ✅ | 「相同方式共享」：**裁一刀就算改编，改编稿要以同样许可发布**。本工具的处理是：照常裁剪，`foot` 里**只写来源、作者、许可名三样**，条款原文留在项目台账里（见下） |
| CC0 / 公有领域 | ✅ | ❌（建议标） | 最省事 |
| CC 标 NC / ND | ❌ | — | 一律不要用 |
| Pexels | ✅ | ❌（建议标） | 不得转售原图、不得暗示照片中的人为你背书 |
| AI 生成（本机） | ✅ | — | 无第三方著作权，**但必须标「示意图」**，且**要人眼过一遍有没有伪文字**（见下第 4 条） |

实务建议：

1. **优先 CC BY / CC0 / 公有领域**。CC0 / 公有领域不强制署名，CC BY 只要求署名，两者都不限制你再授权。
   BY-SA 照常裁剪使用，但 **`foot` 里只写来源、作者、许可名三样**：

   ```json
   "配图：维基共享资源 · N509FZ；CC BY-SA 4.0"
   ```

   **不要抄许可条款原文**。「经裁剪，按相同许可发布」这类话读者看不懂 —— 它是要留档的事实，
   不是要给读者看的信息。裁剪与相同许可记在项目的 `配图署名.md` 台账里，需要追溯时查台账。
   脚注位置宝贵（最多 3 行），留给读者真正需要的来源信息。

   商业投放、品牌稿建议直接改走 Pexels 或自采图——BY-SA 的传染性在商业场景不好处理。
2. **图多的时候只列必须署名的**。`duo` 最多 6 张图，作者全列会让 `foot` 涨到 4 行、把内容区顶小。
   作者列前 2 个 + 「等」，许可名合并成一组（公有领域不强制署名，可以不占行）：

   ```json
   "foot": [
     "配图：维基共享资源 · TrutH SuiTeR、N509FZ 等；CC BY-SA 3.0/4.0",
     "数据来源：教育部《全国教育事业发展统计公报》"
   ]
   ```

   > 脚注**最多 3 行**。一行按卡片内容宽 658px 算，约合 41 个汉字或 76 个西文字符，
   > 超了会自动折行、多占高度、把内容区挤小。
   > 这行不必手写：`node 署名行.js 数据源.json` 会从图里反查台账、拼好这行，
   > 并报出它占多少字宽。**超 41 时它会自动把作者一个个砍下去，直接给出能放进一行的版本**
   > （`duo` 那种一页六张图的，默认 `--作者上限 2` 常常就超了）。
3. **别把开放图库的图当"现场照"**。开放图库的图多半是通用题材，不能暗示成"某某大学实拍"，
   图注写描述性文字。Pexels 的中文语境尤其差（搜"校园"出来基本是欧美校园），用了就在图注写「示意图」。
4. **AI 生成的图（`img/ai/`）按"底纹"用，不按"照片"用**。三条纪律：

   - **只在满幅底图（`bg`）这种位置用**。它压在文字下面、还盖着淡纱，没人会把它当新闻现场。
     拿它去当 `photo` / `hero` 的正文配图，就是在暗示"这是实拍"，不行。
   - **一律标「示意图」**。`署名行.js` 认 `"AI 生成"` / `"AI-generated"` / `"人工智能生成"` 三种写法，
     会自动拼成 `AI 生成底图（示意图）；无第三方著作权` —— **`bg` 的值或图注里带上这几个词就行**。
   - **生成后必须人眼过一遍有没有伪文字**。中英混排的提示词最容易让模型在画面里画出一堆"看着像汉字"的
     笔画，缩到 810 宽就是一团花。**本模型看不了图**，所以这一步只能人来做。
   - 生成尺寸按 **2:3 竖图（1024×1536）**，别按 16:9 —— 画布是 3:4，2:3 只裁约 11%，16:9 要裁掉 58%。

---

### 4.5 用官方媒体图（写时事热点时的正路）

开放图库搜不到"这所学校的现场"——但媒体报道里有。**为报道时事新闻而配的媒体图，
属于《著作权法》第二十四条的合理使用**，条件是四要件全中：

| 要件 | 具体做法 |
|---|---|
| ① 为报道该时事新闻 | 图必须服务这条新闻本身。**拿媒体图当"气氛图"配无关话题 ❌** |
| ② 不可避免 | 能用自由授权图的位置就别用媒体图。全景/示意图优先走 4.1 |
| ③ 指明作者与来源 | 媒体名 + 记者（原文标了就写）/ 原文链接，缺一不可 |
| ④ 不损害权利人权益 | 不裁掉或遮盖署名水印、不用于带货导购 |

工具侧两条命令：

```bash
node 找图.js page "<文章URL>" "文件名" 1600   # 抓 og:image 主图，并打印标题/媒体/作者
node 找图.js url  "<图片直链>" "文件名" 1600   # 页面抓不到主图时的兜底
```

`page` 会顺带把标题、媒体名、作者读出来，直接抄进 `foot`：

```json
"foot": [
  "来源：成都日报、无锡博报、江南大学，2026-09",
  "图片：成都日报（记者 某某）/ 原文链接"
]
```

几个注意：

- **网页抓不到就换一条新闻**。同一件事各家都报了，优先选有 `og:image`（页面主图）的那篇；
  没有 `og:image` 时脚本会退回扫 `<img>`，容易捞到 64px 的栏目小图标（已过滤 `<400px` 的候选）。
- **媒体图不进 `img/` 长期复用**。它是为这一条新闻的合理使用，换话题再用就掉出四要件了。
- **账号开了流量主、接商单后风险上升**。商业场景优先自采或自由授权图。
- **署名行照样占 `foot` 额度**（最多 3 行），媒体名可以合并成一行，别一行一个来源。

---

## 五、字段速查（完整）

### 所有版式通用

| 字段 | 说明 |
|---|---|
| `name` | 输出文件名（仅批量时用，不进图） |
| `layout` | `stat` / `list` / `compare` / `vs` / `cards` / `steps` / `timeline` / `chart` / `bar` / `photo` / `photoFocus` / `duo` / `hero` / `bigword` / `band` / `cover` / `circle` |
| `theme` | `blue` / `red` / `green` / `amber` / `ink`，留空默认 `blue` |
| `shell` | 写 `"full"` 拆掉白卡、满幅铺底（不写＝默认白卡）。**和版式正交，任何版式都能加**；`cover` 自带满幅，不用写。`circle` 也**自带整版排版**（藏共用标题）但**不带满幅**，想要满幅照常写 —— 「自带整版」和「没有白卡」是两件事 |
| `bg` | **满幅底图**路径（相对数据源 JSON 所在目录）。写了它才会挂上"上白下白、中间透"的淡纱；不写就是"无卡的纯色满幅" |
| `badge` | 右上角账号名，默认「小铭想」。≥3 字自动缩字号 |
| `title1` | 主标题第一行（72px，给背景/定性） |
| `title2` | 主标题第二行（58px，带雾带，给反差/悬念） |
| `deck` | 副标题，留空不显示 |
| `foot` | 数组，底部来源/注释，每项一行；留空不显示 |

> 标题写法：**上句不给结论，下句才给**——对应「前 14 字定生死」。五种已验证句型见排版手册第二节。

### 各版式专属字段

| 版式 | 字段 |
|---|---|
| `stat` | `statValue` 主数字、`statUnit` 单位、`statLabel` 说明、`statSubs` 底部 2 条补充 |
| `list` | `listTitle` 标题、`listItems` = `[{ "h": "小标题", "p": "一行解释" }]`，建议 3–4 条 |
| `compare` | `cmpTitle`、`cmpLeftTitle`/`cmpLeftItems`、`cmpRightTitle`/`cmpRightItems`（各 4 条） |
| `timeline` | `tlTitle`、`tlItems` = `[{ "t": "2025", "h": "小标题", "p": "一行解释" }]`，建议 4–5 条 |
| `chart` | `chartTitle`、`legendBar`/`legendLine`、`years`、`counts`、`wealth`、`highlight`（第几根柱，从 0 数）、`countAxisMax`/`wealthAxisMax`、`countUnit`/`wealthUnit`、右轴格式 `wealthAxisFormat`/`wealthLabelFormat`/`wealthDecimals`（见下） |
| `bar` | `barTitle`、`barLabels`、`barValues`、`compareLead`（`\n` 可换行）、`compareA`、`compareMid`（默认 V）、`compareLead2`、`compareB` |
| `photo` | `img`、`cap` 图注 |
| `photoFocus` | `img`、`pills` 胶囊文字（1–2 条）、`cap` |
| `duo` | `img`/`img2` 两个主体图、`quote` 金句、`quads` 四宫格标签（**必须 4 项**）、`quadImgs` 四宫格图（**默认给满 4 项**）、`quadPos` 每格裁切位置（可选） |
| `vs` | `vsTitle` 小标题、`vsMid` 中间徽章文字（默认 `VS`）、左右各一组 `vsLeftTag`/`vsLeftTitle`/`vsLeftItems`/`vsLeftFit` 与 `vsRight*`（各 3 条要点） |
| `cards` | `cardsTitle`、`cards` = `[{ "n": "01", "tag": "要办", "h": "标题", "p": "解释", "warn": true }]`；`n` 留空自动按 01/02/03 编号，`warn: true` 变警示红 |
| `steps` | `stTitle`、`stItems` = `[{ "t": "到校前", "h": "信息前置", "p": "一行解释" }]`，建议 4 条（末级自动反白） |
| `hero` | `img` 主图、`heroTitle` 压在图上的结论、`heroLead` 结论下的补充行（可省）、`heroItems` = `[{ "h": "小标题", "p": "一行解释" }]`，**固定 3 条** |
| `bigword` | `bwTitle` 顶部小标题（可省）、`bwWord` **巨型字**、`bwUnit` 小单位（可省）、`bwNote` 说明、`bwKeys` = `[{ "k": "1.22万", "v": "标签", "warn": true }]`（0–3 个） |
| `band` | `bandTitle` 顶部小标题（可省）、`bands` = `[{ "n": "01", "h": "标题", "p": "一行解释", "tone": "plain", "warn": true }]`，**2–4 条** |
| `cover` | `bg` 满幅底图（**一定要写**）、`cvTag` 顶部小标签、`cvBig` 左对齐大字（**支持行内高亮，用 `<em>…</em>` 包住的那段带主题浅色底衬**）、`cvNote` 大字下的说明、`cvKeys` = `[{ "k": "1.22万", "v": "撤销 / 停招布点", "warn": true }]`，**2–3 项** |
| `circle` | `ccKick` 圆内小标签（可省）、`ccWord` **圆内主词**、`ccUnit` 主词后的小单位（可省，与主词共基线）、`ccNote` 说明、`ccCorners` = `[{ "pos": "tl", "h": "查名单", "p": "新增 7 所，全在中西部" }]`，**1–4 条**，`pos` 取 `tl`/`tr`/`bl`/`br` |

> `band` 的 `tone` 三档：`dark` 主题深色反白 / `soft` 主题浅色 / `plain` 纯白 + 上下细线。
> **不写就按序号深浅交替**（第 1 条深、第 2 条浅…），首尾同深天然收口。`warn: true` 覆盖 `tone`，整条变警示红。
> 色带高度是**等分**的，不按文案长短分配 —— 这样才有"分割"的秩序感。

> `cover` 的三条硬约束：
> ① **`cvBig` 不缩字号**。它按自然折行排，写超了会折第三行顶出画布（其他单行标题都会自动缩）——
>    **宁可让探针报错，也不要静默把字缩小**。自己守"每行 8 汉字、共 2 行"。
> ② **它会藏掉 `title1` / `title2` / `deck`**。写了也不显示，标题就靠 `cvBig`，别再重复写一遍。
> ③ **行内高亮只能用 `<em>`**，且它垫的是 `box-shadow: inset`（不是 `background`）——
>    后者会撑开行高让两行错位。`<em>` 的样式在模板里，别自己加 `style`。

> `circle` 的三条硬约束：
> ① **圆的大小是量出来的，不是写死的**。直径取三者最小：出血宽（卡内 758 / 满幅 810）、内容区高、
>    四角还站得下人的上限。所以**满幅下圆会小一圈**（实测 776px），两边各露 17px 底 —— 设计内的，不是 bug。
> ② **`ccWord` 和 `ccUnit` 一起量宽**（超过圆内 434px 就按 2px 一档缩主词，下限 56px），
>    所以主词写长了会连带单位一起变小，观感不如直接换个短词。
> ③ **角注 `p` 要守 2 行**（卡内一行约 11 汉字、满幅只有约 7 汉字）。**满幅下圆更大、四角更小**，
>    角注得比卡内版更短 —— `海报探针.js` 会打出 `角注离圆=`，**负数就是压进圆里了**。
> 另外它和 `cover` 一样会藏掉 `title1` / `title2` / `deck`；右侧两个角注默认右缩 12px，让开卡片右上角的账号角标。

### 右轴不是钱？改 `wealthAxisFormat`

`chart` 是双轴：左边柱子、右边折线，两条轴各管一个指标。**右轴默认按金额显示**（`$500tn`），
因为这套版式最早是给「富豪人数 vs 财富总额」做的。换成别的单位要覆盖三个字段：

| 字段 | 默认 | 说明 |
|---|---|---|
| `wealthAxisFormat` | `"${v}tn"` | 右轴**刻度**文字，`{v}` 替换成刻度值 |
| `wealthLabelFormat` | `"${v}tn"` | 折线**数据点**上的数字 |
| `wealthDecimals` | `1` | 折线数据点保留几位小数 |

例：把右轴改成「在学研究生人数（万人）」——

```json
"wealthAxisMax": 500,
"wealthAxisFormat": "{v}",
"wealthLabelFormat": "{v}",
"wealthDecimals": 0
```

> **折线的数字是压在柱子身上的**，模板已经给它们垫了一层白底，任何柱色都读得清。
> 早期版本没有这层底，荧光绿压深蓝柱基本看不清。

### 图片字段的路径规则

- 写**相对路径**时，按「数据源 JSON 所在目录 → 脚本目录 → 当前工作目录」依次查找。
  最常见的做法：数据和 `img/` 放同一个目录，JSON 里写 `"img/xxx.jpg"`。
- 写**绝对路径或网址**都支持。但**外链图不会被 SVG 内嵌**（导出时会在命令行列出来）。
- 找不到的图**不会静默变白**：会在命令行列出来，页面上渲染成虚线占位块（四宫格是「缺图」小方块）。
- **`bg`（满幅底图）是图片字段里唯一的例外**：它的名字里没有 `img`，用来识别图片字段的
  「键名含 `img`」规则会漏掉它。**`batch出图` / 体检 / 署名行三处都已显式加了 `bg`** ——
  以后再加这类"名字里没 `img` 的图片字段"，记得往这三处的白名单里各补一条
  （`批量出图.js` 的绝对化、`度量.js` 的 `expectImgs`、`署名行.js` 的 `collectImages`）。
  漏了不会报错，症状是底图在 `.pages/` 里 404、体检打「图 0/0 张」。

### `quadImgs` / `quadPos` 怎么用

`duo` 的四宫格**不给图就是「首字大卡」**（彩色底 + 1 个大字 + 标签），**给了图就是「图 + 标签」**：

```json
{
  "layout": "duo",
  "img": "img/EEG_Recording_Cap.jpg",
  "img2": "img/Clean_room.jpg",
  "quads": ["脑机科学与技术", "半导体工艺与装备", "具身智能", "海洋智能与无人技术"],
  "quadImgs": [
    "img/Brain-computer_interface_experiment.jpg",
    "img/Semiconductor_Wafer_of_Microelectronics.jpg",
    "img/TOPIO_3_3.jpg",
    "img/RoboSub_diver.jpg"
  ],
  "quadPos": ["12% center", "center", "center", "72% center"]
}
```

- **`quadImgs` 默认给满四张**。不给图就是「首字大卡」——四个色块各压一个大号汉字，
  那是模板的**兜底样式**，不是常用样式；给图才是「图 + 标签」。
  真要空着（比如那格本来就是抽象概念、实在找不到对得上的图），**四格都不给**，让整行统一成首字卡；
  只给一两张会一眼看出是漏配。
- 标签写短一点（2–4 个汉字最好），它显示在图下方，19px、超长会折行。
- **四宫格格子约 152×142（近方形）**，`object-fit: cover` 的保留率只看原图长宽比：
  4:3 横图留 81%、3:2 留 72%、**2:3 竖图留 70%**——竖图照样能放（居中裁，主体在画面中部即可），
  别信「竖图只剩一条」的说法。真正吃画幅的是 `duo` 的**主体**：画框 320×272 而且是
  `object-position: top center`，**16:9 宽幅图只留 66% 且靠上对齐**，选宽幅图要确认主体在上半部。
- `quadPos` 支持 `object-position` 的写法（`0% center` 取最左、`100% center` 取最右、`center` 居中裁）。
  导 SVG 时只会保留**左/中/右三档**，要精修就在 Figma 里拖。
- `duo` 一共要 6 张图（2 主体 + 4 宫格），**脚注额度会被挤掉**：`foot` 最多 3 行，
  署名行写不下就只列主体图的作者 + 「等」，完整名单放在项目的配图署名台账里。

---

## 六、五套配色

配色在 `主题.js` 里统一定义，**HTML 和 SVG 两条路共用这一份**，所以不会脱节。
`theme` 字段填名字即可：

| key | 名字 | 适合 |
|---|---|---|
| `blue` | 蓝白 | 数据分析、趋势、理性陈述 |
| `red` | 中国红 · 金榜 | 高考政策、放榜、喜报 |
| `green` | 墨绿 · 理性 | 长期发展、理性建议 |
| `amber` | 暖橙 · 米色 | 志愿选择、避坑、提醒 |
| `ink` | 墨黑 · 极简 | 时评、热点、态度 |

稳妥的搭配：

| 选题类型 | 推荐版式 | 推荐配色 |
|---|---|---|
| 高考政策 / 放榜 | `timeline`、`stat` | `red` |
| 志愿选择 / 避坑 | `compare`、`list` | `amber` |
| 数据分析 / 趋势 | `chart`、`bar` | `blue` |
| 大学热点 / 时评 | `stat`、`photo` | `ink` |
| 长期发展 / 理性建议 | `list`、`compare` | `green` |
| 封面 / 头图（一句狠话） | `cover`、`bigword`、`circle` | `ink`（最压得住底图/色块）、`blue`（干净） |

---

## 七、导出 SVG（拖进 Figma 二次设计）

```bash
node 导出SVG.js 数据.json svg输出
```

得到 N 个 SVG + 一个 `_预览.html`（浏览器打开一次看完）。**用鼠标直接把 SVG 拖进 Figma 画布**，进去之后：
文字是可编辑文本框、图形是可编辑矢量、**照片是已内嵌的 `<image>`**（自带圆角裁切，不用手动置位）。

- 图片内嵌前会按目标宽度压一遍（`sips`，最长 1200px、JPEG 72）。不压的话原图直塞能到几十 MB，Figma 会卡死。
- 外链图（http / data）不内嵌，导出时在命令行列出来，SVG 里留虚线占位框。
- **没登记的版式以前会静默退回 `photo`**（导出结果和 PNG 对不上、还不报错）。现在会警告并列出漏掉的版式名。
  加新版式时记得两边一起补：`贴图模板.html` 的 `LAYOUTS` + `导出SVG.js` 的 `CONTENT`。
- **SVG 的 `<text>` 不会自动折行**（HTML 会），长句要么调短、要么在 `导出SVG.js` 里过一遍 `TBlock()` 自己切行。
- **导出后建议跑一遍几何校验**：`node svg校验.js svg输出` —— 它把导出的 SVG 内联进一个页面，
  用 `getBBox()` 量每个元素的真实边界，报出「顶出画布 / 盖住脚注 / 压到主标题」。字号调大后最容易出这个问题。
- **导出前后一共四道自检**，各管一件事、互不替代：

  | 校验 | 管什么 | 跑法 |
  |---|---|---|
  | 出图自带的体检 | 内容放不放得下 / 真图渲染了几张 / 有没有缺图 | `./批量出图.sh 数据.json 出图目录`，**自动**，零额外开销 |
  | `越界探针.js` | 元素**看得见地**顶出卡片边界 | 两步：`node 越界探针.js .pages` 生成 `_probe_*.html` → 逐个 `--dump-dom`，从 `<title>` 里读 `over=OK` |
  | `海报探针.js` | 海报手法**真的生效**（字号 / 色块 / 倾斜 / 出血 / 底图有没有解码） | 同上两步，读 `<title>` 里的 `POSTER …` |
  | `svg校验.js` | SVG 侧的真实几何边界（`getBBox()`） | `node svg校验.js svg输出` |

  > **体检和越界探针不是一回事**：体检只看 `overflow !== visible` 的元素，抓不到"布局没溢出、
  > 但元素自己画到卡外面去了"这种。手算高度的版式（`band` / `bigword` / `cover` / `circle`）尤其要跑越界探针。
  >
  > 两个探针都得**把探针页和被探页面放在同一目录**（图是相对路径，挪走就全 404），并且
  > 要从 **`<title>`** 里取结果 —— 直接正则整个 `--dump-dom` 会先匹到页面 `<script>` 里的探针源码。
- 导入后建议做三件事：① 字体缺失就把标题换成**思源宋体 Source Han Serif**、正文换**苹方 PingFang SC**；
  ② 整张图做成 Component，以后复制实例改文字；③ 图层已按 `背景` / `账号角标` / `主标题` / `内容区-xxx` / `来源注释` 分好组。

**什么时候用哪条路**：要发布 → PNG；要二次设计（换配色、调间距、加元素、做系列变体）→ SVG。
两条路读的是同一个 JSON。

---

## 八、生成总览

```bash
node 生成总览.js 数据.json 输出目录 "标题"
```

在输出目录生成 `_总览.html`：3 列网格引用同目录 PNG，每张下方标 `layout · theme`，
缺图会标红框并在命令行列出。**张数多了可以加 `--cols=N` 改列数**（比如 15 张用 5 列更紧凑）：

```bash
node 生成总览.js 数据.json 输出目录 "标题" --cols=5
```

要 PNG 版总览，直接跑配套脚本：

```bash
./总览截图.sh 输出目录          # 默认 1204 宽、1.5 倍
./总览截图.sh 输出目录 1204 2   # 需要更大就调宽度和倍率
./总览截图.sh 输出目录 1938 1   # 5 列时的宽度（= 列宽 1850 + 两侧 44px 内边距）
```

> **改列数就要同步改截图宽度**：总览页是固定列宽的网格，列数变了页面宽度也变。
> 宽度不对会横向留白或把最后一列挤掉。量高与截图**必须同宽**，理由同上。

产出 `输出目录/_总览.png`。**别写死 `--window-size` 的高度**：总览页高随张数变化
（几张一行、共几行），写死不是底部留一大片底色就是最后一行被截断。脚本先让页面报出
`documentElement.scrollHeight`，再按这个数字截图——顺带一提，量高和截图必须同宽，
宽度决定网格列宽和换行，进而决定页高。

---

## 九、二次开发

| 想改什么 | 改哪里 |
|---|---|
| 配色 | `主题.js` 里对应主题对象的色值（HTML 侧全部走 CSS 变量，加第 6 套只需加一个 key） |
| 骨架常量（卡片内边距、圆角、标题字号） | `贴图模板.html` 顶部 CSS + `导出SVG.js` 里的常量（两处要同步） |
| 加一种新内容区版式 | `贴图模板.html` 的 `LAYOUTS` 对象 + `导出SVG.js` 的 `CONTENT` 对象，各写一个函数 |
| 账号角标 | 数据里的 `badge` 字段；连底色一起改就改 `主题.js` 的 `badge` |
| 输出尺寸 | `批量出图.sh` 顶部的 `W` / `H` / `SCALE` |
| 满幅外壳（白卡 / 圆角 / 淡纱） | `贴图模板.html` 的 `.poster.full` / `.full.hasbg` / `.own-title` 三条 CSS；`导出SVG.js` 的 `isFullShell()` |
| 满幅时的出血距离 | `贴图模板.html` 里 `-76px` 三处 + `导出SVG.js` 的 `bleedBox()`（卡内 50px ↔ 画布 76px 两档，**两处必须同步**） |
| 「自带整版排版」的版式（藏共用标题、`content` 从顶部放开） | `贴图模板.html` 的 `render()` 里 `ownShell` 那一行 + `导出SVG.js` 的 `buildSVG()` 里同名变量（`cover` / `circle` 两个）。**注意它和满幅是两件事**：`cover` 两个都自带，`circle` 只自带整版 |
| `circle` 的圆直径 / 角注宽 | `贴图模板.html` 的 `render()` 里那段 `--ccd` / `--ccw` 量算 + `导出SVG.js` 的 `contentCircle()` 里同公式（`NOTE_H` / `PAD` / `CW_MIN` / `dx0` 四个常量必须一致）+ CSS 里 `.ly-circle` 的 `--ccd` / `--ccw` 兜底值 + `INNER_W = 434` 对 `.cc-in` 的 `width` |

> **加新版式时四条容易漏的**：
> ① `导出SVG.js` 的 `CONTENT` 漏了会**静默退回 `photo`**（现在会告警，但要自己看命令行）；
> ② 版式里如果有"必须单行"的标题，要**显式调 `fitText`**（写在 `render()` 里），否则会折行把卡片顶高；
> ③ 如果有出血元素，记得同时打 **`.bleed` 类**，否则越界探针会全报成越界；
> ④ 如果版式要**「同时看宽和高才算得出来的尺寸」**（`circle` 的圆就是这样），CSS 给不出，只能在
>    `render()` 里量完写回 CSS 变量 —— **记得两条渲染路各算一次，并把常量对齐**（见 `docs/贴图排版手册.md` 第 24 条）。

> 模板里删 `<script>` 要小心：`./vendor/echarts.min.js`（图表）和 `./主题.js`（配色）少任何一个，
> 对应部分会**静默空白**。模板已内置兜底提示：`主题.js` 缺失整页红框报错，`echarts` 缺失在图表位显示虚线框。

---

## 十、常见问题

**Q：出图后是空白 / 图表没出来？**
A：看两处：`vendor/echarts.min.js` 是否存在、`主题.js` 是否在脚本目录里。

**Q：命令行说成功，但图的字节数都一样？**
A：这是最隐蔽的故障——说明全部渲染成了同一个页面，多半是模板选错了。
`批量出图.js` 现在会按数据里有没有 `layout` 字段自动选模板，正常不该出现。
自查一行：`stat -f "%z" 输出目录/*.png | sort | uniq -c`，出现同一个尺寸重复 N 次就是它。

**Q：`node 批量出图.js` 报没权限启动 Chrome？**
A：改用 `./批量出图.sh`（由终端直接调 Chrome，绕开子进程限制）。

**Q：四宫格的图没显示，是四个「缺图」小方块？**
A：图片路径解析失败。检查 `quadImgs` 的路径是否相对于数据源 JSON 所在目录；
命令行里会列出具体是哪一项找不到。

**Q：满幅底图 `bg` 没显示？**
A：按这个顺序查：

1. **是不是没写满幅？** `bg` 只在 `"shell": "full"` 或 `layout: "cover"` 时才会渲染。
   只写 `bg` 不写 `shell`，图不显示也不报错 —— 这是最常见的漏。
2. **路径对不对？** 规则同其他图：相对「**数据源 JSON 所在目录**」。比如数据在项目根、
   图在 `img/ai/xx.jpg`，就写 `"img/ai/xx.jpg"`。
3. **是不是 404 在 `.pages/` 里？** `bg` 这个名字里没有 `img`，早先版本会被"按键名含 `img`"
   的规则漏掉。现在三处（绝对化 / 体检 / 署名行）都已显式加白名单；**症状是体检打「图 0/0 张」**，
   去看 `.pages/` 目录里底图是不是没被拷进去。

**Q：加了 `"shell": "full"`，但看起来还是白卡？**
A：只有背景层变了 —— 圆角没了、卡变透明、底图铺满。`badge`、标题、脚注**都还在原位**，
   所以乍看变化不大。**想连标题一起交出去、整张就是一张封面，用 `cover` 版式**（它自带满幅，
   并会藏掉 `title1` / `title2` / `deck`）。
   另外 JSON 里要写 `"shell": "full"`，`full` 两个字母也是字符串，别写漏引号。

**Q：`circle` 版式的圆，加了满幅反而变小了？**
A：不是 bug，是**四角（负形）在反过来限制它**。圆的直径取三者最小：出血宽（卡内 758 / 满幅 810）、
   内容区高、**四角还站得下角注的上限**。满幅时圆想顶到画布边，但那样四角就只剩一点点、
   角注放不下 —— 于是圆被压到 **776px** 左右，两边各露 17px 底。这是刻意的：
   **这个版式的负形和正形一样重要**，宁可圆小一圈也不吃掉四角。
   顺带一句：满幅下角注也变窄（卡内 208px → 满幅 150px），**角注文案要按满幅收**。
   验的办法是 `海报探针.js`（它专门多打了一行 `角注离圆=`，负数就是压进圆里了）。

**Q：我看不了图（或没法逐张打开检查），怎么确认照片真渲染进去了，而不是虚线占位块？**
A：两条命令行手段，都不用肉眼看图。

① **裁中心切片比字节数**。真照片的高频细节压不动，平色块能压到极小：

```bash
sips -c 600 600 --cropOffset 760 620 输出/某张.png --out /tmp/c.png
stat -f "%z" /tmp/c.png
```

一批里**留一张纯文字卡当对照**（比如 `timeline`，通常 30–50 KB）。带图卡的中心切片
一般在 200 KB 以上；如果只有几十 KB，就是照片没进去，或者被 `photoFocus` 的白圆整个盖住了。

> ⚠️ 这一条**只能当提示，不能当判决**。两种情况会误报：
> ① `photoFocus` 的正中本来就被 480px 白圆盖住，中心切片天然是平色；
> ② 白色/光滑场景（实验室、白墙教室）本身熵就低，真照片也压得很小。
> 拿不准就走下面 ③ 的 A/B，那才是决定性的。

② **无头 Chrome 数渲染后的 `<img>`**：

```bash
"/Applications/Google Chrome.app/Contents/MacOS/Google Chrome" \
  --headless=new --no-sandbox --disable-gpu --virtual-time-budget=5000 \
  --dump-dom "file://$PWD/某张.html"
```

两个必须注意的点：
- **路径要绝对**（`file://$PWD/...`）。写成相对路径 Chrome 会空跑、什么都不输出，
  看着像"页面里没有 img"，其实是没打开页面。
- 本机 Chrome 起不了自身沙箱，**要带 `--no-sandbox`**，否则 SIGTRAP、无输出。

页面 `<script>` 里的模板字面量本身也含 `<img class="${cls}" ...>`，会被一起数进去。
**运行期注入的真图 `src` 是绝对文件路径**（`src="/Users/.../img/xxx.jpg"`，不是 `file://`），
所以数 `src="/` 开头的才准；同时搜 `class="... miss"` 应为 0（有就是缺图）。
`duo` 在补了 `quadImgs` 之后应该是 **6 张**真图（2 主体 + 4 宫格）。

③ **A/B 对照（最硬的一条）**。同一张卡做两份数据：一份图路径正常、一份把 `img` 改成不存在的文件名，
各出一张 PNG 比总字节数：

```
AB_图存在.png   1482 KB
AB_图缺失.png    870 KB      ← 差 612 KB，照片确实画上去了
```

差得明显就是渲染了；两张一样大说明图根本没进去（那次是走了「缺图」虚线占位）。

> ⚠️ 别用 `grep -c placeholder` 判断——模板的 CSS/JS 里到处是 `placeholder` 字样，
> 每张页面都会返回同一个数字，看起来像"每张都缺图"，其实什么都没说明。

**Q：页脚上面那几行字好像被挤到了 / 内容好像压着页脚？**
A：跑 `./检查溢出.sh 输出目录`。它逐张量 `.content` 的 `scrollHeight - clientHeight`：

```
01_双轴柱线图_蓝白     ✅ 放得下  内容区 +0px  卡片 +0px  画布 +0px
09_双主体加四宫格_墨绿  ⚠️ 溢出   内容区 +17px  卡片 +0px  画布 +0px
```

超出几个 px 会落在卡片 40px 的底部内边距里，不构成遮挡（脚本按 4px 以内算通过）；
再多就会盖到页脚文字上。`duo` 是最容易出这个问题的版式——它的主体图、金句、四宫格
都是固定高度。解法：**页脚少一行**、金句改短，或者换版式。

**Q：`photoFocus` 的图被白圆压住了？**
A：换一张主体不在正中的图，或改用 `photo` 版式。

**Q：文字被裁掉 / 折行难看？**
A：看第三节的文案长度红线。模板会自动缩字号，但缩到下限就放开折行了。

**Q：时间轴的日期被折成两行？**
A：日期列宽 86px，模板已设 `white-space: nowrap`，超长标签会向左溢出、不会折行。
（更早的版本会折，而且同一个 `9月10日` 折、`9月11日` 不折——差在一个字符的宽度上。）
保险做法是把 `tlItems[].t` 控制在 5 个字符内：`9月10日`、`2014` 都行，`2025年10月` 太长。

**Q：`chart` 右轴显示成 `$500tn`，但我画的不是钱？**
A：用 `wealthAxisFormat` / `wealthLabelFormat` / `wealthDecimals` 覆盖，见第五节末尾。

**Q：`_总览.png` 底部留一大片底色，或者最后一行被截掉了？**
A：这是写死截图高度的典型后果——总览页高随张数变化（几张一行、共几行），
固定值不可能都对。跑 `./总览截图.sh 输出目录`：它先让页面报出 `documentElement.scrollHeight`，
再按这个数字截。手动截的话务必注意**量高和截图要用同一个宽度**，宽度决定网格列宽和换行。

**Q：改用 `总览截图.sh` 后量出来的页高明显偏小（截图截掉一行）？**
A：检查探针页是不是被挪到了临时目录。`_总览.html` 里图片是相对路径 `./xxx.png`，
挪走后会全部 404、`img` 高度塌成 0，量出来的页高就成了"少一行"的。探针页必须和 PNG 同目录。

---

## 十一、踩过的坑

见 [`docs/贴图排版手册.md`](docs/贴图排版手册.md) 第八节「二十四个坑」，每条都写清了解法。最值得先看的三条：

1. **验收信号是"各图字节数互不相同"**——整批图一样大比白屏更难发现。
2. **截图必须写临时文件名再 mv**，否则"文件大小稳定"会立刻成立，拿到的是上一轮的旧图。
3. **别用 glob 扫 `.pages/` 决定截哪些页面**，旧页面会被一起截，白出一批过期图。

> **改模板前必看同一条**：JSON 和 SVG 是**两条独立的渲染路**（`贴图模板.html` 走 CSS、
> `导出SVG.js` 写死常量）。同一个改动必须两边都做，而且**字号要分别验** ——
> SVG 的 `<text>` 不会自动折行，同一句话在 HTML 能折两行、在 SVG 会横着顶出画布。

---

## 十二、文件说明

| 文件 | 作用 |
|---|---|
| `SKILL.md` | 作为 WorkBuddy 技能使用时的入口（触发条件 + 铁律） |
| `贴图模板.html` | 模板本体。浏览器直接打开就是一个设计台，可切版式/配色、导出单张 PNG |
| `主题.js` | 5 套配色定义（UMD，浏览器挂 `window.THEMES`，Node 走 `module.exports`） |
| `批量出图.js` | 批量生成页面 + 无头 Chrome 截图（纯 Node，含模板自动选择、图片路径解析） |
| `批量出图.sh` | 两段式版本：Node 生成页面 → Shell 逐个调 Chrome 截图 |
| `度量.js` | 出图时顺带体检：溢出量 / 真图数 / 缺图数写进页面 `<title>`，随截图一起回来 |
| `导出SVG.js` | 导出可编辑 SVG，本地图 base64 内嵌 |
| `找图.js` | 维基共享 / NASA / Pexels 三个图源的搜索与下载；`page` / `url` 两条命令抓官方媒体图（见 4.5） |
| `查署名.js` | 按 Commons 标题补查作者与许可（只读元数据，不下载图片） |
| `署名行.js` | 从数据源扫出真正用到的图 → 查台账 → 拼好脚注那行署名，并报出占多少字宽 |
| `探测分类.js` | 批量探测若干 Commons 分类有没有图、有多少张，用来快速判断哪个分类值得挖 |
| `列分类.js` | 列出某个 Commons 分类下（含一层子分类）的全部图片文件名，用来挑图 |
| `生成总览.js` | 总览页（只出 HTML）；`--cols=N` 可改列数（默认 3 列） |
| `总览截图.js` / `总览截图.sh` | 把总览页存成 PNG：先量真实的页高再截，不留白不截断（配套使用，`.sh` 是入口） |
| `预览截图.sh` | 把 SVG 预览页 `_预览.html` 存成 PNG（复用 `总览截图.js` 的量高逻辑） |
| `检查溢出.js` / `检查溢出.sh` | 出图后回读校验：逐张量内容区有没有顶出高度（配套使用，`.sh` 是入口） |
| `越界探针.js` | 量化「元素看得见地顶出卡片」——体检抓不到这类；生成探针页后交给 `--dump-dom` |
| `海报探针.js` | 读 `computedStyle` 确认海报手法（字号 / 色块 / 倾斜 / 出血 / 底图解没解码）真的生效；`circle` 版式额外打一行 `角注离圆=`（负数＝角注压进圆里了） |
| `svg校验.js` | 把导出的 SVG 内联进页面，用 `getBBox()` 量真实边界，报顶出画布 / 盖脚注 / 压标题 |
| `vendor/echarts.min.js` | 图表版式依赖 |
| `docs/贴图排版手册.md` | 骨架常量、版式分界、海报手法拆解、字段表、二十四个坑（**改模板前先看**） |
| `docs/视觉海报案例学习笔记.md` | 视觉海报案例怎么拆、学到什么、落到哪个版式、怎么验 |
| `examples/` | 样板集数据（15 张 / 14 版式）+ 真实热点示例数据 + 示例配图 + 输出预览 |
